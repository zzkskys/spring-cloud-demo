spring:
  application:
    name: nacos-consumer

  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        file-extension: yml

management:
  endpoints:
    web:
      exposure:
        include: '*'

